// preload.js
window.addEventListener("DOMContentLoaded", () => {
  // This code runs before your web page loads.
  // You can expose safe APIs to your app here if needed.
  console.log("Preload script loaded.");
});
